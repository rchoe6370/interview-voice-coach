# SECRETS — never push these (repos are often public at judging)
.env
.env.local
*.pem
*.key
credentials.json
secrets/

# Python (eval scripts, tooling)
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Node
node_modules/
dist/
build/

# Runtime data — regenerate via scripts, don't bloat the repo
data/*.db
data/*.db-journal
*.log

# OS noise
.DS_Store
Thumbs.db

# Local backup video (large; keep on disk, not in git)
*.mp4
*.mov
testdata/
